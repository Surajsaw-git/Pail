;9. Write a program to subtract two 8-bit numbers and verify the result using GDB.

section .text
global _start
_start:
    mov al,9
    sub al,4
    add al,'0'
    mov [res],al
    mov eax,4
    mov ebx,1
    mov ecx,res
    mov edx,1
    int 0x80
    mov eax,1
    int 0x80
section .data
res db 0
